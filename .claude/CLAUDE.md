- When removing code, delete it instead of commenting it out
- Add type annotations for return values where it doens't add extra complexity to do
- After making code changes intelligently decide whether to run tests to verify correctness

# Work Configuration
@~/.claude/CLAUDE.work.md
